## 1. Implementation
- [x] 1.1 Extend translation settings schema/migration for definition backfill + definition-translation toggles (defaults off).
- [x] 1.2 Add Options UI toggles, with translation toggle visible but disabled when backfill is off.
- [x] 1.3 Gate definition backfill requests and definition-translation behavior using the new settings.
- [x] 1.4 Update unit tests for settings and backfill behavior.
- [x] 1.5 Run `npm test` and `npm run lint`.
